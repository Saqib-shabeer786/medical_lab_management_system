<%= form_with(model: patient_report) do |form| %>
  <% if patient_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(patient_report.errors.count, "error") %> prohibited this patient_report from being saved:</h2>

      <ul>
        <% patient_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :case_id, style: "display: block" %>
    <%= form.number_field :case_id %>
  </div>

  <div>
    <%= form.label :perameter_id, style: "display: block" %>
    <%= form.number_field :perameter_id %>
  </div>

  <div>
    <%= form.label :result, style: "display: block" %>
    <%= form.text_field :result %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
